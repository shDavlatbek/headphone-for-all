#import "GeneratedPluginRegistrant.h"

// C ABI of the Rust core's native discovery backend (HfaBonjourDiscovery.swift). The symbols are
// exported by the app's Rust library (the cargokit pod framework rust_lib_headphone_for_all,
// linked with -force_load, which Runner links through the Pods xcconfig).
#import "../../../core/hfa-ffi/include/hfa_discovery.h"
